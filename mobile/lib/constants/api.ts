// API Configuration
// For development, use your local machine's IP address instead of localhost
// when testing on physical devices or emulators

export const API_URL = process.env.EXPO_PUBLIC_API_URL || 'http://localhost:3001'

// For testing on physical devices, replace with your computer's local IP:
// export const API_URL = 'http://192.168.1.XXX:3001'
